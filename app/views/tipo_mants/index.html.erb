<h1>Listing tipo_mants</h1>

<table>
  <tr>
    <th>Tipo</th>
    <th>Preventivo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tipo_mants.each do |tipo_mant| %>
  <tr>
    <td><%= tipo_mant.tipo %></td>
    <td><%= tipo_mant.preventivo_id %></td>
    <td><%= link_to 'Show', tipo_mant %></td>
    <td><%= link_to 'Edit', edit_tipo_mant_path(tipo_mant) %></td>
    <td><%= link_to 'Destroy', tipo_mant, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tipo mant', new_tipo_mant_path %>
